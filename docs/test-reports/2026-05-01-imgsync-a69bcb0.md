# imgsync 테스트 리포트

**대상 커밋**: `a69bcb0` (main, PR #6 머지 직후)
**실행 일시**: 2026-05-01 01:26 KST
**실행 환경**: darwin/arm64, Go 1.25.6, Docker 28.0.4, Helm + kind 설치됨

## 0. 요약 (Executive Summary)

| 카테고리 | 결과 | 시간 | 비고 |
|---|---|---|---|
| 정적 분석 — `go vet` | **PASS** (exit 0) | <1s | 0개 경고 |
| 정적 분석 — `gofmt -l` | **PASS** | <1s | 0개 미포맷 파일 |
| 스트리밍 가드 | **PASS** (exit 0) | <1s | `io.ReadAll`/`bytes.NewBuffer(body)` 핫패스 0건 |
| Helm lint | **PASS** (exit 0) | <2s | 1 chart linted, 0 failed (info: icon 권장) |
| Helm 차트 구조 테스트 | **PASS** (exit 0) | <3s | 5개 시나리오 모두 green |
| 단위 테스트 (race detector + count=1) | **PASS** (exit 0) | 138.7s 패키지 합 | 17 패키지 모두 ok |
| 통합 테스트 (`-tags integration`, sniffer) | **PASS** 22/22 | 28.152s | testcontainers Postgres ×2 |
| 프로덕션 바이너리 빌드 | **PASS** | <5s | 18MB, `imgsync --help` 5개 서브커맨드 |
| E2E (이번 실행 안 함) | — | — | 게이트 `IMGSYNC_E2E=1`. 과거 PASS 기록 §7 |

전체 **failing 0건**, 코드/차트/포맷 회귀 0건.

## 1. 환경 / 메타데이터

```
Go        : go1.25.6 darwin/arm64
Docker    : 28.0.4 (desktop-linux 컨텍스트, 데몬 가용)
Helm      : /opt/homebrew/bin/helm (lint/template 정상)
kind      : /opt/homebrew/bin/kind (E2E 가용)
HEAD      : a69bcb0f25a748abddd0754171ac0bc775a0b580 (main)
모듈      : github.com/nineking424/imgsync
LOC       : .go 6,867 (production 3,125 + tests 3,742)
테스트 파일: 32개
테스트 함수: 98개 (Test* + Benchmark*)
```

## 2. 정적 분석 결과

### 2.1 `go vet ./...`
```
vet exit=0
```
모든 패키지 통과, 경고 0건.

### 2.2 `gofmt -l`
```
gofmt issues=0
```
스캔된 모든 .go 파일이 정규 포맷을 만족. (PR #6 첫 푸시에서 lint CI가 잡았던 `internal/sniffer/sniffer.go`, `internal/sniffer/traceid.go` 두 건은 `90b2adb`에서 수정 완료된 상태.)

### 2.3 스트리밍 가드 (`scripts/check-streaming.sh`)
```
OK: no io.ReadAll or bytes.NewBuffer(body) buffering in streaming hot paths
exit=0
```
v1 핵심 invariant: 1GB 파일도 RSS<250MB로 스트리밍 처리. 핫패스에서 전체 버퍼링 패턴이 검출되지 않음.

## 3. Helm 차트 검증

### 3.1 `helm lint deploy/helm/imgsync`
```
==> Linting deploy/helm/imgsync
[INFO] Chart.yaml: icon is recommended
1 chart(s) linted, 0 chart(s) failed
exit=0
```

### 3.2 `helm template` 렌더링 출력 (스팟 체크)
```
ServiceAccount: imgsync
ConfigMap     : imgsync
ConfigMap     : imgsync-sniffer
Service       : imgsync
Deployment    : imgsync           (replicas=1, image=imgsync:0.1.0)
Deployment    : imgsync-sniffer   (replicas=1, image=imgsync:0.1.0)
Job           : imgsync-migrate-1
```

ConfigMap 핵심 환경변수 — Task 13 픽스 검증:
```
SNIFFER_SRC_PROTOCOL: "localfs"
SNIFFER_DST_PROTOCOL: "localfs"
```
(이전 `"fs"` 기본값은 worker 레지스트리에 없는 프로토콜이어서 dead-letter 발생 → `localfs`로 정정 완료.)

### 3.3 차트 구조 테스트 (`deploy/helm/imgsync/tests/template_test.sh`)
```
==> helm lint                               PASS
==> helm template (default values)          PASS
==> helm template (replicaCount=8)          PASS
==> probes + env + secret ref               PASS
==> migrate Job hook annotations            PASS
==> migrationJob.enabled=false              PASS
PASS: helm chart structural tests green
exit=0
```

## 4. 단위 테스트 (`go test ./... -race -count=1`)

전체 17 패키지 모두 PASS. exit=0.

| 패키지 | 결과 | 시간 |
|---|---|---|
| `internal/backoff` | ok | 1.650s |
| `internal/cli` | ok | 1.627s |
| `internal/db` | ok | 11.220s |
| `internal/eval` | ok | 19.347s |
| `internal/ftpserver` | ok | 2.221s |
| `internal/health` | ok | 11.174s |
| `internal/hostcap` | ok | 7.621s |
| `internal/jobs` | ok | 9.212s |
| `internal/sniffer` | ok | 25.412s |
| `internal/sourcedb` | ok | 5.424s |
| `internal/sources/ftp` | ok | 2.190s |
| `internal/sources/localfs` | ok | 2.396s |
| `internal/sweeper` | ok | 12.479s |
| `internal/transfer` | ok | 1.239s |
| `internal/transports/ftp` | ok | 2.399s |
| `internal/transports/localfs` | ok | 1.267s |
| `internal/worker` | ok | 21.863s |
| **합계** | **17/17 PASS** | **138.741s 합** |

**`-race` 플래그 활성** — race detector 검출 0건.
**`-count=1` 플래그 활성** — 캐시 미사용, 모든 테스트 실제 실행.

## 5. 통합 테스트 (`go test -tags integration ./internal/sniffer/...`)

**전체**: 22 RUN / 22 PASS / 0 FAIL / 28.152s
**의존성**: testcontainers — PostgreSQL 컨테이너 ×2 (imgsync DB + source DB)

### 5.1 시나리오별 상세 (모두 PASS)

| # | 테스트 | 시간 | 의도 |
|---|---|---|---|
| 1 | `TestEnqueue_InsertsNewRow` | 2.24s | 기본 enqueue → `transfer_jobs` 1 row + `transfer_events` 1 row |
| 2 | `TestEnqueue_SecondCallIsNoop` | 1.44s | 동일 trace_id+dst 재호출 = no-op (`UNIQUE` + `ON CONFLICT DO NOTHING`) |
| 3 | `TestEnqueue_DifferentDstSameTraceIDInsertsBoth` | 1.65s | 동일 trace_id, 다른 dst = 2 row 모두 insert |
| 4 | **`TestS0_PollingOverlapNoDuplicate`** | 3.33s | 윈도우 overlap 으로 같은 row 두 번 sniff → 1 row 유지 |
| 5 | **`TestS1_CrashRecoveryNoLossNoDup`** | 2.48s | sniffer 크래시 → 재시작 → 손실 0/중복 0 |
| 6 | **`TestS2_TieBreakBatchCorrectness`** | 2.25s | 동일 ts 10 row + batch=3 → 4 batch 후 모두 1번씩 |
| 7 | **`TestS3_QueryTimeoutLeavesWatermarkUnchanged`** | 2.42s | source DB query timeout → watermark 유지 + enqueue 0건 |
| 8 | `TestQuery_WindowAdvancesAcrossSameTS` | 1.14s | 같은 ts 안에서 watermark가 pk로 진행 |
| 9 | `TestQuery_TieBreakAcrossDigitBoundary` | 1.10s | pk 9→10 lex 경계 회귀 (Task 10 추가) |
| 10 | `TestQuery_ExtraColumnsRenderNullAsEmpty` | 1.12s | NULL extra column → 빈 문자열 |
| 11 | `TestQuery_BatchSizeZeroErrors` | 0.00s | batch_size=0 입력 거부 |
| 12 | `TestQuery_BiasExcludesRecentRows` | 1.08s | `NOW() - bias_sec` 윈도우로 최근 row 제외 |
| 13 | `TestRunOnce_EnqueuesAllAndAdvancesWatermark` | 2.17s | 메인 루프 1 tick = enqueue 전체 + watermark 진행 |
| 14 | `TestRunOnce_NoRowsLeavesWatermarkUnchanged` | 2.15s | 빈 윈도우 = watermark 그대로 |
| 15 | `TestStateRepo_LoadMissingReturnsZeroValue` | 1.07s | sniffer_state 없을 때 zero value |
| 16 | `TestStateRepo_UpsertThenLoad` | 1.11s | upsert → load 라운드트립 |
| 17 | `TestStateRepo_UpsertOverwritesExisting` | 1.11s | upsert 덮어쓰기 |
| 18 | `TestTraceID` | 0.00s | `${table}-${pk}` 결정성 |
| 19 | `TestDstPath_ShadowSuffixApplied` | 0.00s | `.imgsync_shadow_v1` suffix 부착 |
| 20 | `TestDstPath_ShadowOff` | 0.00s | shadow=false → suffix 미부착 |
| 21 | `TestDstPath_MissingKey` | 0.00s | template 미존재 키 = 에러 |
| 22 | `TestDstPath_EmptyPatternRejected` | 0.00s | 빈 pattern 거부 |

핵심: **S0~S3 4개 시나리오 모두 PASS** (Sniffer Tasks 9~10에서 추가). C8~C11 critical test cases의 자동화 lock.

## 6. 빌드 검증

```
$ go build -o /tmp/imgsync-report/imgsync-bin ./cmd/imgsync
(exit 0)

$ /tmp/imgsync-report/imgsync-bin --help
imgsync: file transfer queue (Go + PostgreSQL)
Available Commands:
  completion  Generate the autocompletion script for the specified shell
  enqueue     Insert a transfer job (idempotent on trace_id, dst)
  help        Help about any command
  migrate     Apply forward-only SQL migrations from a directory
  sniffer     Poll a source DB and enqueue new transfer jobs
  worker      Drain the transfer_jobs queue

$ ls -lh imgsync-bin
18M imgsync-bin
```

## 7. E2E 테스트 — 이번 실행 안 함, 과거 결과 기록

**왜 이번에 안 돌렸나**: `IMGSYNC_E2E=1` 게이트 + kind cluster 셋업이 필요. 부트스트랩 5~10분 + 테스트 자체 수십 분. 즉시 측정 불가능.

| 테스트 | 마지막 PASS | 커밋 | 런타임 | 비고 |
|---|---|---|---|---|
| **C7 throughput scale-out** | Week 3 Task 5 | (Helm 머지 시점) | 미기록 | 2→8 replica 선형성 ≥3.2× 검증 |
| **F5 dirty-state recovery** | Week 3 Task 6 | (Helm 머지 시점) | 미기록 | mid-flight kill + helm rollback + reinstall |
| **C5' sniffer self-audit** | Sniffer Task 12 | `5232788` | **27.82s** | 1000 row → enqueued=1000, distinct trace_id=1000, dead=0 |

C5'는 Task 12 implementer가 이번 세션 중 로컬 kind 클러스터에서 확인했고, 그 후 cleanup 데드락 픽스 (`b76fc7c`)와 chart 기본값 픽스 (`1cb10d8`)를 추가했지만 두 픽스 모두 테스트 결과에 영향 안 줌:
- `b76fc7c` = 테스트 끝 cleanup 단계의 LIFO 데드락 (실제 검증은 이미 끝난 후 발생). 픽스로 CI 타임아웃 절약.
- `1cb10d8` = chart 기본값. 테스트는 `--set` 으로 명시적 override 했었기 때문에 동일 결과 보장.

## 8. CI 결과 (PR #6, GitHub Actions)

| Run | Job | 결과 | 시간 |
|---|---|---|---|
| `25176092531` | lint-and-test | **fail** (gofmt 2건) | 24s |
| `25176092531` | e2e | skipping | 0s |
| `25176170470` | lint-and-test | **pass** (gofmt 픽스 후) | — |

링크:
- https://github.com/nineking424/imgsync/pull/6 (머지 완료, merge commit `a69bcb0`)

## 9. Coverage Map 매핑 (test plan 기준)

설계 문서 (`~/.gstack/projects/.../nineking-main-eng-review-test-plan-...md`)의 25개 행 중:
- 행 1~16 (v1 base): 단위 + integration + E2E로 모두 커버 (Week 1~3 plan에서 닫음)
- 행 17~25 (sniffer): 단위 (`TestQuery_*`, `TestTraceID`, `TestDstPath_*`) + integration (S0~S3) + E2E (C5')로 모두 커버

Critical Test Cases (C0~C11 + C5'):
- **C0/C1/C2/C3/C6/C7/F5**: v1 base 작업에서 닫음 (`internal/eval/*` 단위 + e2e)
- **C5'/C8/C9/C10/C11**: sniffer 작업에서 닫음 (S0~S3 integration + C5' E2E)
- **C4/C5**: REPLACED 처리됨 (NiFi 비교 제거 결정)

## 10. 결론

`a69bcb0` 시점 기준으로:
1. 정적 분석 / 포맷 / 스트리밍 가드 / 단위 / 통합 / Helm 차트 / 빌드 — **모두 green**
2. 통합 테스트는 testcontainers PostgreSQL 위에서 실제 SQL을 실행 (mock 0%) → S0~S3 전체 PASS
3. E2E (kind 클러스터 필요)는 이번에 재실행하지 않음 — 마지막 PASS 결과를 §7에 명시
4. 회귀 위험 신호 없음. 다음 단계는 코드보다는 운영 — production shadow start, baseline metric 측정

## 11. 재현 명령

```sh
# 정적 분석
go vet ./...
find . -name '*.go' -not -path './.worktrees/*' | xargs gofmt -l
./scripts/check-streaming.sh

# Helm
helm lint deploy/helm/imgsync
./deploy/helm/imgsync/tests/template_test.sh

# 단위 (race + count=1)
go test ./... -race -count=1 -timeout 5m

# 통합 (Docker 필요, testcontainers Postgres)
go test -tags integration -timeout 5m -count=1 -v ./internal/sniffer/...

# E2E (kind 클러스터 + IMGSYNC_E2E=1)
make e2e-up
make e2e-throughput     # C7
make e2e-dirty-state    # F5
make e2e-sniffer        # C5'
```
